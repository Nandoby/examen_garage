<header>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container-fluid">
            <a class="navbar-brand" href="{{ path("home") }}">BIACAR</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01"
                    aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarColor01">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link {{ app.request.get('_route') == 'home' ? 'active' }}"
                           href="{{ path("home") }}">Accueil
                            <span class="visually-hidden">(current)</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {{ app.request.get('_route') == 'ventes' ? 'active' }}"
                           href="{{ path("ventes") }}">Ventes</a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path("about") }}"
                           class="nav-link {{ app.request.get('_route') == 'about' ? 'active' }}">A propos</a>
                    </li>
                </ul>
                {% if app.user %}
                    <ul class="navbar-nav me-3">
                        <li class="nav-item"><a href="{{ path('logout') }}" class="nav-link">Déconnexion</a></li>
                    </ul>
                {% else %}
                    <ul class="navbar-nav me-3">
                        <li class="nav-item"><a class="nav-link" href="{{ path('register') }}">Inscription</a></li>
                        <li class="nav-item"><a class="nav-link" href="{{ path('login') }}">Connexion</a></li>
                    </ul>
                {% endif %}
            </div>
        </div>
    </nav>
</header>
